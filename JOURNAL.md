# Agent Omega — Journal

## 2026-07-11 — v2.6.1 task-quality lifecycle hardening

**Changed:** Added the task-quality lifecycle plugin and its router/sidecar/setup integration, the bottom-of-window operator runner, and startup fail-closed compatibility enforcement for the required engine protocol. Release documentation now identifies the matching Windows engine asset and checksum, explains the intentional old/upstream-engine rejection, and records the platform boundary.

**Verified:** `npm test` completed with **161 passing, 0 failing** tests. The final lifecycle regressions cover exact-message artifact-review handoff, completion suppression while review is pending, durable scope revocation while executions settle, and chained receipt-winning CAS conflicts without stale approval reactivation. A 103-test engine regression slice produced 102 passes plus one unrelated HTTP test that crossed its command's five-second timeout by 4.59 ms; that complete file then passed **19/19** with the repository's normal 30-second test budget. The focused engine SDK suite completed with **20 passing, 0 failing**, engine typecheck passed, and the compiled engine smoke test passed. `dotnet build -c Release` completed successfully with the existing `MSB3277` assembly-resolution warning. Earlier real packaged Windows-app runs recorded one direct task against the final transform-resolved agent, one attested child read, durable receipts, and zero pending execution. HSS and same-model CRAP were both exercised through the clean-room artifact path: valid structured reviews completed, malformed or internally contradictory results failed closed, and workspace inspection rejected extra-tool and missing-artifact cases. The final local-model CRAP run completed execution but returned an incomplete structured review, which closed the lifecycle as `artifact-review-failed` rather than authorizing completion. The live runner was inspected in the Windows app and displayed model, token, context-limit, and provider-quota fields (unreported values render explicitly rather than being invented). The source and packaged engine copies had the same SHA-256: `4a1c01e4c3192fb7aaac924e1aeccf9190936ad35a092e22fc9260b618eb3799` (`0.0.0-omega-task-quality-202607111649`).

**Not verified:** macOS v2.6 runtime/engine packaging, the optional paid cloud/API browser suite, and a new live-model artifact-review result after the final lifecycle hardening. The engine's full repository suite remained active but did not complete inside a ten-minute local run; the changed HTTP/API surface, typecheck, build smoke test, and the complete Agent Omega suite passed. Earlier Windows execution lifecycle and negative/fail-closed review paths were verified; local reviewer output remained nondeterministic. v2.6 therefore releases a Windows engine asset only; macOS users must remain on a release with a matching macOS engine asset.

## 2026-07-07 — Fixed slow "spin-up" + invisible subagent activity (3 fixes, all proven)

**Discussed:** Two complaints — (1) a "quick test" took ~10 min for the small driver model to spin up the larger specialist before it even started, (2) the app only ever showed a frozen `task: agent testing` line, no view of what the subagent was doing. Checked whether the slowness was the app (it wasn't — app+harness fire in ~50ms). Root cause: the model-swap layer's idle-pin setting **pins** a loaded model but does NOT **preload** it, so after any host reboot/service-restart the model sits unloaded and the FIRST request eats the full cold-load (driver ~21s, specialist ~194s) plus the driver→specialist hand-off — that's the 10 min. Second problem: the ACP bridge only carries the PARENT/driver session's stream; child (subagent) sessions never cross it, so the UI had nothing to show.

**Decided:** Ship all three approved fixes. (1) Warm each host's pinned model automatically on every model-service start. (2) Surface the subagent's live internal activity in the app. (3) Show a "waking up…" status while a model is genuinely cold. Fix 1 lives host-side (infra), Fixes 2–3 in the app. Left the final live desktop click to the user (he must restart the app to get the new code anyway).

**Changed:**
- **Fix 1 (keep models warm) — host-side infra, no repo commit.** A service drop-in on each inference host runs a small warm-up script whenever the model service (re)starts — covering both boot AND a bare service restart (a boot-only trigger would miss the latter), backgrounded so the restart returns instantly rather than blocking on the load. The script waits for the model server to bind its HTTP port, then fires a 1-token completion to trigger and block on the load of that host's pinned model.
- **Fix 2 + 3 (subagent visibility + warming status) — in `sidecar.mjs` + `ui/app.html`** (already deployed to `bin/Release`, byte-identical). Sidecar: `childSessions` map + `slimSubPart()` + `forwardEngineEvent()` subscribe to the engine `/event` SSE and emit `{type:'subagent', phase:created|link|part|end}` frames over the existing WS broadcast. app.html: nested `.subagent` panel under the task (reasoning + tool lines + streamed answer, spinner→✓/✕), plus two-level "waking the model…" warming lines (subagent-level 15s + top-level turn-start), cancelled by first activity.

**Proven:** Fix 1 — restarted both hosts live: warm ran in the background (driver 21s / specialist **194s** → http 200), and the next real request landed on the warm model in **0.3–0.6s** (vs minutes). Non-blocking confirmed (service `active` immediately, warm-up detached). Fixes 2/3 — every seam on real data: real `/event` delegation capture → shipped `forwardEngineEvent` transform (13/13) → real `app.html` DOM render (24/24) + screenshot showing the specialist's live reasoning/tools/answer where the frozen line used to be. Warming appears at 15s of silence and clears on activity (S2/S3/S4).

**Open-next:** the user **restarts Agent Omega** (the running instance is pre-change) — that both delivers Fixes 2/3 and lets him watch the live panel on his next delegation. Offered a deeper full-live end-to-end (real delegation, frames over the wire) if he wants belt-and-suspenders instead of leaving the last visual confirm to him.

## 2026-07-06 — Setup system Phases 1–3 shipped; preview copy ready for e2e test

**Discussed:** Finishing the built-in setup/help system end-to-end. Mid-build, Fable hit its WEEKLY rate limit — the user said use two Opus 4.8 reviewers in its place. Whether the daily driver's binary should change (no — keep it Phase-1 until an explicit prod push). How to give a true first-run test without touching the real vault.

**Decided:** Ship all 3 phases to `main` as SOURCE only; the daily driver stays on its current (Phase-1) build until the user approves. Test everything on an isolated copy driven by cheap gpt-5.5 (daily driver untouched). Substitute the Fable 3×/phase review with pairs of Opus 4.8 adversarial reviewers (begin/mid/end). Deliver a self-contained **preview** build that isolates HOME (empty vault) + XDG_CONFIG_HOME (blank config) via a launcher, so the user gets a genuine first-run.

**Changed:**
- **Phase 1 (finish + fixes) — commit `0ae5a09`.** `/customize` bounded setup agent (9-tool plugin, auto-reload, finish→handback). Fixed live: the app runs the bin/ COPY of sidecar.mjs (edits weren't reaching it); the engine's /event SSE carries NO tool events → rewired the watcher onto the ACP sessionUpdate stream (keyed on tool name); mode-drops-after-reload P1 (snapshot curAgent before start() resets it); stale-flag + watcher false-positive P2s. Files: `config-template/opencode/{agent/setup.md, setup/index.js+lib.mjs, command/customize.md+init.md}`, `sidecar.mjs`.
- **Phase 2 (first-run onboarding) — commit `bea3a8f`.** Sidecar `computeOnboard()` detection + `onboardKey`/`onboardLocal`/`onboardSkip` WS handlers + a non-model key-entry card (`ui/first-run.js`) that validates (reuses the plugin's validateKey via a dynamic import of the live lib.mjs), stores to vault, reloads, sets the session model, hands off to the setup agent. `ui/app.html` (include + onWs hook), `setup.md` (FIRST_RUN line). Opus reviewers caught: cloud-baseURL false-positive in detection, onboardLocal not setting the session model (greeted on wrong model), unbounded /models fetch, multi-window status leak, card busy-freeze on WS reconnect — all fixed.
- **Phase 3 (skin polish) — commit `8db3c0d`.** `ui/first-run.js` redesigned: A/O wordmark, WELCOME box-label, CRT phosphor + Modern glass (via --ac + scoped style block), provider chips, entrance animation. Opus reviewers caught: the keyboard-modal was INERT (`__ftpModalOpen` is a getter-only accessor → assignment is a silent no-op; now composes the getter like crt-settings), short-viewport overflow hiding Continue (backdrop scrolls), engine-down banner stacking guard, Modern focus/input bleed, a11y (role=dialog, aria-label, focus-on-show, Esc=dismiss), contrast, reduced-motion — all fixed.
- **Preview deliverable:** a separate preview copy of the repo — full dotnet Release build (Phase 1+2+3) + `run-preview.cmd` (isolates HOME+config). Reset blank for a fresh first-run.

**Proven (real WebView2 app, not just harnesses):** fresh-install detection → onboarding card served → real key validated/stored/reloaded → setup agent greets on gpt-5.5 (cloud path); local path with a mock server (session model correctly set); full `/customize` run adding a model + skill (persisted, round-tripped off disk); both skins render correctly; short-viewport Continue reachable; keyboard-modal actually blocks the app underneath. Daily driver verified pristine throughout (no first-run.js in its bin, no marker).

**Open-next:** the user runs `run-preview.cmd` and tests end-to-end (first-run + push on /customize + both skins). On approval, the "prod push" = rebuild the daily driver from `main` (Phase-1 binary → full). Delete `agent-omega-preview` when done. Deferred earlier features still on the list: task/todo UI surfacing, background monitor, MCP enable, schedule wakes (share an out-of-loop sidecar-runner architecture).
