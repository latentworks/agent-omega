# Memory index

Your curated cross-session notes — one line per memory: `- [Title](slug.md) — hook`.
Each memory is a file in this folder (`memory/<slug>.md`); the `remember` tool and the memory
format are described in your system prompt. Empty until you add entries.
