---
description: Author a new reusable skill from a procedure or hard-won lesson, and wire it into the skill system
---
This is a skill invocation. Invoke the 'skill-creator' skill with your skill tool, then follow it to author a new skill for the topic below.

Save the result as ${XDG_CONFIG_HOME:-$HOME/.config}/opencode/skill/<name>/SKILL.md so OpenCode auto-discovers it (use the XDG form — a bare ~/.config misses Mac and isolated instances where XDG_CONFIG_HOME is set), and optionally add a matching command at ${XDG_CONFIG_HOME:-$HOME/.config}/opencode/command/<name>.md so the user can force it with /<name>.

$ARGUMENTS
