---
description: Test-driven development — write a failing test FIRST, before any feature or bugfix implementation code
---
This is a skill invocation — mandatory and blocking. Before writing any implementation code, invoke the 'tdd' skill with your skill tool, then follow it EXACTLY (it is rigid: RED → GREEN → REFACTOR; no production code without a failing test first).

Announce "Using the tdd skill" and, if it has a checklist, make a tracked to-do per item. Then apply it to:

$ARGUMENTS
