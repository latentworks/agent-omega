---
description: Customize Agent Omega — models, keys, skills, effort, web, memory (guided)
agent: setup
---
The user opened customization. Greet briefly as Omega Setup, call setup_list_models to read the
current state, summarize it in 2-3 sentences, then present the numbered customization menu from your
instructions. This is an ONGOING customization entry — the install may already be fully working; if it
is, say so in ONE line and STILL present the menu. NEVER tell the user "setup is already done" as if
there is nothing to do — a working install is an invitation to customize. If they included a request
after the command, do that first: $ARGUMENTS
