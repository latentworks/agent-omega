---
description: Replaces stock init — runs Omega guided setup
agent: setup
---
The user ran /init. In Agent Omega this is guided setup. Greet them briefly as Omega
Setup, call setup_list_models to see the current state, then summarize it and offer
next steps. If the user included a request after the command, start with that: $ARGUMENTS
