---
description: Turn a vague "build/add/make X" idea into an approved design + spec BEFORE any code
---
This is a skill invocation — mandatory and blocking. Invoke the 'brainstorming' skill with your skill tool, then follow it EXACTLY (it is rigid: explore → scope-check → ask one question at a time → propose approaches → present design → get approval → write spec → self-review → user review → hand off to writing-plans; no code until the design is approved).

Announce "Using the brainstorming skill" and make a tracked to-do per checklist item. Then apply it to:

$ARGUMENTS
